# 아키텍처 의사결정 기록 (ADR)

## ADR-001 팀 제출 정보를 MDX 컬렉션 + App Router 카드 목록으로 노출

- 상태: 승인
- 날짜: 2026-02-12

### 배경
- 해커톤 참가팀 정보와 제출 프로젝트를 문서 형태로 관리하고, 사이트에서 시각적으로 확인할 수 있는 목록 화면이 필요했습니다.
- 기존 코드베이스는 Fumadocs MDX 컬렉션(`source.config.ts`)을 사용하고 있어 동일한 방식으로 확장하는 것이 자연스럽습니다.

### 결정
- 팀 데이터 저장소를 `contents/team` 디렉토리의 개별 MDX 문서로 정의합니다.
- `team` 컬렉션 스키마에 프로젝트 제출 정보(`projectName`, `projectSummary`, `projectUrl`, `repositoryUrl`, `demoUrl`, `submittedAt`, `order`)를 포함합니다.
- App Router 경로 `/teams`를 추가하고, `@shadcn/card` 컴포넌트 기반의 이미지 카드 목록 UI로 렌더링합니다.
- 목록 정렬은 `order` 오름차순을 우선 적용하고, 동순위는 팀명 기준으로 정렬합니다.
- 카드 UI는 `CardAction + Badge + Button` 패턴으로 통일해 프로젝트/팀 소개와 외부 이동 동선을 단일 액션으로 단순화합니다.
- 저장소 링크는 메인 버튼 우측의 GitHub 아이콘 버튼으로 분리해, 프로젝트 보기와 저장소 보기를 동시에 빠르게 접근할 수 있게 구성합니다.
- 목록 레이아웃은 `md` 이상 구간에서 3열 그리드를 사용해 팀 카드 밀도를 높입니다.

### 근거
- MDX 파일 기반 운영은 비개발자/운영자의 수정 접근성이 높습니다.
- 카드 UI는 팀별 프로젝트를 빠르게 스캔하기에 적합하고, 이미지 없는 경우에도 대체 배경으로 시각적 일관성을 유지할 수 있습니다.
- 기존 Fumadocs 컬렉션 파이프라인을 재사용하여 구현 복잡도를 낮출 수 있습니다.

### 결과
- `/team`에서 팀/프로젝트를 카드 형태로 확인할 수 있게 되었습니다.
- 기존 `/teams` 경로는 `/team`으로 리다이렉트되어 호환성을 유지합니다.
- 팀 데이터는 `contents/team/*.mdx`에서 독립 문서 단위로 관리됩니다.

### 추후 고려사항
- 팀 상세 페이지(`/teams/[slug]`)를 추가해 MDX 본문을 직접 렌더링할 수 있습니다.
- 운영 편의를 위해 제출 링크 유효성 검증(빌드 시 체크)을 추가할 수 있습니다.

## ADR-002 오픈라우터 설정 문서의 Codex 연결 방식 명시

- 상태: 승인
- 날짜: 2026-02-19

### 배경
- 참가자들이 Codex에서 OpenRouter를 연결하는 방식(전용 provider 설정 vs OpenAI provider 호환 설정)을 혼동할 수 있습니다.
- 설정 경로와 환경변수 조합이 문서에 명확히 고정되어야 제출물 검증 시 재현성이 높아집니다.

### 결정
- `contents/docs/openrouter-setup.mdx` 문서의 Codex 섹션을 `codex-cli (@openai/codex)` 기준 안내로 교체합니다.
- Codex 연결 절차는 아래 두 가지를 모두 지원하도록 문서화합니다.
  - 방법 A: `OPENROUTER_API_KEY` 환경변수와 `~/.codex/config.toml`의 `openrouter` provider 설정
  - 방법 B: `OPENAI_BASE_URL`을 `https://openrouter.ai/api/v1`로 지정하는 OpenAI provider 호환 설정
- Codex 섹션에는 지원 형태(커스텀 provider / 기본 provider 엔드포인트 오버라이드)와 설정 위치(`~/.codex/config.toml`, `.codex/config.toml`)를 함께 명시합니다.
- 방법 A 예시는 `model = "openai/gpt-5.2"`를 포함하고, 필요 시 `wire_api` 설정 가능성을 주석으로 안내합니다.

### 결과
- 참가자는 자신의 운영 방식에 맞춰 Codex 연결 전략을 선택할 수 있습니다.
- 운영진/참가자 모두가 동일한 설정 근거를 문서에서 확인할 수 있어 공정성과 추적 가능성이 강화됩니다.

## ADR-003 오픈라우터 설정 문서의 Claude Code 연결 방식 구체화

- 상태: 승인
- 날짜: 2026-02-19

### 배경
- 기존 Claude 섹션은 개략 설명 중심이라 실제 연결 시 필요한 환경변수/파일 위치/검증 절차의 재현성이 낮았습니다.
- 같은 문서 내 Codex 섹션은 상세 포맷(지원 형태/설정 위치/방법 A/B)을 갖추고 있어, 도구 간 가이드 품질을 맞출 필요가 있었습니다.

### 결정
- `contents/docs/openrouter-setup.mdx`의 Claude 섹션을 Codex와 동일한 포맷으로 재작성합니다.
- OpenRouter 연결 필수 환경변수 4개(`OPENROUTER_API_KEY`, `ANTHROPIC_BASE_URL`, `ANTHROPIC_AUTH_TOKEN`, `ANTHROPIC_API_KEY=""`)를 명시합니다.
- 설정 방식은 아래 두 가지를 모두 제공하도록 문서화합니다.
  - 방법 A: 셸 프로필 기반 영구 적용(zsh/bash/fish)
  - 방법 B: Claude Code `settings.json`의 `env` 필드 기반 파일 설정
- Windows PowerShell 설정, `claude` 실행 절차, `/status` 기반 검증, 자주 발생하는 오류 해결 절차를 함께 포함합니다.

### 결과
- Claude Code 사용자는 OS/설정 선호도에 따라 동일한 기준으로 OpenRouter 연결을 재현할 수 있습니다.
- 문서 내 Codex/Claude 섹션의 구조와 문장 톤이 통일되어 학습 비용이 낮아집니다.

## ADR-004 오픈라우터 설정 문서의 Cursor 연결 절차 구체화

- 상태: 승인
- 날짜: 2026-02-19

### 배경
- 기존 Cursor 섹션은 API Key/Base URL/Model 나열 수준으로, 실제 UI 경로와 단계가 부족해 따라 하기 어려운 상태였습니다.
- Codex/Claude 섹션이 이미 절차 중심으로 정리되어 있어, Cursor도 같은 수준의 구체성이 필요했습니다.

### 결정
- `contents/docs/openrouter-setup.mdx`의 Cursor 섹션을 절차형 가이드로 재작성합니다.
- 지원 형태를 OpenAI API Key + Base URL override 방식으로 명시합니다.
- OpenRouter 안내 기준으로 아래 설정 절차를 문서화합니다.
  - `Settings -> Cursor Settings -> Models` 이동
  - `API Keys`에서 OpenAI API Key 활성화, OpenRouter 키 입력, `Override OpenAI Base URL` 활성화
  - Base URL `https://openrouter.ai/api/v1` 설정 및 모델 추가 단계 안내
- 추가 참고 링크로 OpenRouter의 Cursor 모델 사용 공지 페이지를 포함합니다.

### 결과
- Cursor 사용자는 UI 클릭 경로와 입력 항목을 기준으로 OpenRouter 연결을 빠르게 재현할 수 있습니다.
- 도구별 섹션 품질이 일관되어 문서 학습/검증 비용이 낮아집니다.

## ADR-005 오픈라우터 설정 문서의 OpenCode 연결 방식 구체화

- 상태: 승인
- 날짜: 2026-02-19

### 배경
- 기존 OpenCode 섹션은 API Key/Base URL/Model 항목만 제시해 실제 CLI 연결 순서를 따라가기가 어려웠습니다.
- Codex/Claude/Cursor 섹션이 절차형 포맷으로 정리되어 있어, OpenCode도 동일한 수준의 구체성과 톤이 필요했습니다.

### 결정
- `contents/docs/openrouter-setup.mdx`의 OpenCode 섹션을 절차형 가이드로 재작성합니다.
- OpenCode 연결 핵심 흐름(`/connect` -> OpenRouter 선택 -> API Key 입력 -> `/models` 선택)을 명시합니다.
- `opencode.json` 예시를 아래 2가지로 포함합니다.
  - 모델 추가 예시(`provider.openrouter.models.<model-id>`)
  - 모델별 provider 라우팅 옵션 예시(`order`, `allow_fallbacks`)

### 결과
- OpenCode 사용자는 명령 중심 절차와 설정 파일 예시를 기준으로 OpenRouter 연결을 빠르게 재현할 수 있습니다.
- 오픈라우터 설정 문서의 도구별 섹션이 동일한 형식으로 정렬되어 사용성과 검증성이 개선됩니다.

## ADR-006 오픈라우터 설정 문서의 직접 연결 미지원 도구 명시

- 상태: 승인
- 날짜: 2026-02-19

### 배경
- 도구별 지원 범위가 변하면서 일부 항목의 연결 가능 여부가 실제와 다르게 인식될 위험이 있었습니다.
- 특히 Gemini CLI, Windsurf, Antigravity는 OpenRouter API Key 직접 연결 미지원 상태를 명확히 고지할 필요가 있었습니다.

### 결정
- `contents/docs/openrouter-setup.mdx`에 Gemini CLI 섹션을 추가하고, Windsurf/Antigravity 섹션을 미지원 안내 형태로 변경합니다.
- 세 도구 모두에 "현재 OpenRouter API Key 직접 연결 미지원" 상태를 명시합니다.
- 오해를 줄이기 위해 기존 연결 절차/입력값 나열을 제거하고 지원 여부 중심으로 정리합니다.

### 결과
- 참가자/운영진이 지원 범위를 동일하게 인지할 수 있어 설정 실패와 검증 혼선을 줄일 수 있습니다.
- 문서가 최신 도구 지원 상태를 더 정확히 반영하게 됩니다.

## ADR-007 오픈라우터 설정 문서의 범위/공통입력 정리

- 상태: 승인
- 날짜: 2026-02-19

### 배경
- 문서 상단에 행사장에서 키를 개별 발급한다는 안내가 이미 있어, 본문의 별도 키 발급 절이 중복 정보를 만들고 있었습니다.
- 참가자 입장에서 어떤 도구가 즉시 사용 가능한지 문서 첫 구간에서 빠르게 판단하기 어려웠습니다.
- 공통 입력값의 모델 예시가 고정 문자열로 제시되어 최신 모델 확인 경로를 놓칠 수 있었습니다.

### 결정
- `contents/docs/openrouter-setup.mdx`의 문서 범위 섹션에서 도구 목록을 `사용 가능`과 `사용 불가능(지금은 OpenRouter에 바로 연결할 수 없는 도구)`으로 분리합니다.
- 사용 불가능 그룹은 `Gemini CLI`, `Windsurf`, `Antigravity`로 고정하고, 나머지는 사용 가능 그룹으로 명시합니다.
- 중복되는 `OpenRouter API Key 발급` 섹션을 제거합니다.
- 도구 공통 입력값의 Model ID 안내는 OpenRouter 모델 목록 페이지(`https://openrouter.ai/models?order=newest`) 참조 방식으로 변경합니다.

### 결과
- 참가자는 문서 초반에서 지원 가능 여부를 즉시 판별할 수 있습니다.
- 키 발급 절차 중복이 제거되어 문서 구조가 단순해집니다.
- 모델 선택 기준이 최신 목록 링크로 통일되어 유지보수성과 정확성이 향상됩니다.

## ADR-008 오픈라우터 설정 문서의 미지원 도구 섹션 통합 배치

- 상태: 승인
- 날짜: 2026-02-19

### 배경
- 도구별 설정 가이드에서 미지원 도구가 개별 번호로 흩어져 있어, 사용 가능한 도구 흐름이 중간에 끊기는 문제가 있었습니다.
- 참가자가 먼저 따라야 할 설정 절차(지원 도구)와 참고 정보(미지원 도구)를 분리해 가독성을 높일 필요가 있었습니다.

### 결정
- `contents/docs/openrouter-setup.mdx`의 3번 섹션에서 미지원 도구를 하나의 하위 번호(`현재 지원 불가능한 도구`)로 통합합니다.
- 통합된 미지원 도구 하위 섹션은 3번 섹션의 가장 하단에 배치합니다.
- 해당 하위 섹션에는 `Gemini CLI`, `Windsurf`, `Antigravity`의 OpenRouter API Key 직접 입력 연결 미지원 상태를 함께 명시합니다.

### 결과
- 사용 가능한 도구 설정 순서(Codex/Claude/Cursor/OpenCode/Careti)가 연속적으로 정리되어 따라 하기 쉬워집니다.
- 미지원 도구 정보는 별도 묶음으로 확인할 수 있어 문서 스캔 속도와 이해도가 개선됩니다.

## ADR-009 제출 스킬의 다중 에이전트 발견 경로 통합

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- 제출 스킬 설치 안내가 Codex 중심(`skills/hackathon-submission` URL)으로만 작성되어 Claude Code/Gemini CLI 사용자는 별도 매핑 규칙을 직접 해석해야 했습니다.
- 해카톤 운영 기준에서는 참가자 도구 선택이 자유로우므로, 동일 스킬을 3개 에이전트에서 바로 발견할 수 있는 경로 표준화가 필요했습니다.

### 결정
- 레포에 `hackathon-submission` 스킬을 아래 경로로 배치합니다.
  - `.agents/skills/hackathon-submission`
  - `.claude/skills/hackathon-submission`
- Gemini CLI는 Workspace alias 규칙에 따라 `.agents/skills`를 그대로 사용합니다.
- `contents/docs/how-to-participate.mdx`의 제출 준비 섹션을 다중 에이전트 기준으로 재작성합니다.
  - 레포 로컬 복사 설치 절차
  - GitHub installer(`install-skill-from-github.py`) 기반 설치 절차
  - 설치 후 Codex/Claude/Gemini 재시작 안내
- 동기화 방식은 심볼릭 링크 대신 복사 기반을 기본으로 안내해 Windows/권한 제약 환경에서도 동일 절차를 유지합니다.

### 결과
- 참가자는 사용 에이전트(Codex/Claude/Gemini)와 관계없이 동일한 스킬 이름/구조로 제출 자동화를 실행할 수 있습니다.
- 설치 가이드가 도구별 분기 없이 단일 절차로 정리되어 제출 준비 시간이 단축됩니다.

## ADR-010 문서 페이지 수집에서 참가자 증빙 assets 디렉터리 제외

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- `contents/docs` 하위 참가자 프로젝트에는 증빙 파일 보관용 `assets` 디렉터리가 존재하며, 내부 `README.md`는 폴더 설명 용도로만 사용됩니다.
- 현재 docs 컬렉션이 `**/*.{md,mdx}` 전체를 수집하면서 `assets/**/README.md`도 페이지로 해석했고, `title` frontmatter 누락으로 빌드가 실패했습니다.
- 증빙 자산 파일은 문서 라우팅 대상이 아니라 보조 자료이므로 페이지 schema 검증에서 분리할 필요가 있습니다.

### 결정
- `source.config.ts`의 docs 수집 패턴에 제외 규칙 `!**/assets/**`를 추가합니다.
- `assets` 하위의 마크다운 파일은 문서 페이지 컬렉션에서 제외하고, 페이지 schema(`title` 필수) 검증 대상에서 분리합니다.
- 문서 페이지 수집 범위는 일반 문서(`*.md`, `*.mdx`)에 유지하되 자산 디렉터리만 예외 처리합니다.

### 결과
- 참가자 증빙용 `assets/**/README.md`가 frontmatter 없이 존재해도 docs 빌드가 중단되지 않습니다.
- 실제 문서 페이지에만 schema 검증이 적용되어 빌드 안정성과 문서 운영 분리가 개선됩니다.

## ADR-011 바이브 코딩 결과의 전용 카드 리스트 라우트 도입

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- 참가 결과 문서는 `contents/docs/vibe-coding/**`에 누적되지만, 현재는 개별 문서 경로를 직접 알아야 열람할 수 있습니다.
- 운영자/참가자 관점에서 제출 결과를 한눈에 스캔할 수 있는 목록형 진입점이 필요했습니다.

### 결정
- 전용 라우트 `/vibe-coding`을 추가해 `docsSource.getPages()`에서 `"/docs/vibe-coding/"` prefix를 가진 문서만 필터링해 카드 리스트로 표시합니다.
- 카드에는 문서 메타데이터(`title`, `description|summary`)와 경로 기반 식별자(`team`, `project`)를 함께 노출합니다.
- 카드 CTA는 각 결과 문서 상세 경로(`/docs/vibe-coding/...`)로 이동하도록 고정합니다.
- 결과가 없는 경우 빈 상태와 단일 다음 행동(제출 가이드 이동)을 제공합니다.
- 상단 내비게이션에 `결과` 항목(`/vibe-coding`)을 추가해 접근성을 높입니다.

### 결과
- 바이브 코딩 제출 결과를 카드 리스트 형태로 빠르게 탐색할 수 있습니다.
- 결과 문서 저장 구조(`contents/docs/vibe-coding/**`)를 유지하면서 조회 UX를 개선합니다.

## ADR-012 바이브 코딩 결과 화면의 중복 소개 문구 제거

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- `/vibe-coding` 화면은 제목과 카드 리스트 자체로 목적이 명확하며, 소개 문구가 반복적으로 느껴질 수 있습니다.
- 운영 요청으로 특정 소개 문구를 화면에서 제외할 필요가 있었습니다.

### 결정
- `/vibe-coding` 상단의 소개 문구 `"참가자가 제출한 결과 문서를 카드 리스트로 확인할 수 있습니다."`를 제거합니다.
- 상단 영역은 제목과 결과 건수(`총 n건`) 중심으로 단순화합니다.

### 결과
- 화면의 정보 밀도가 높아지고 중복 텍스트가 줄어듭니다.
- 사용자 요청 문구 제외를 명시적으로 충족합니다.

## ADR-013 제출 스킬 결과 문서의 불필요 섹션 제거와 조건부 렌더링

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- 기존 `hackathon-submission` 템플릿은 `결과 문서` 표현과 제출 준비 고정 설명 문구를 사용해 제목/설명이 장황하게 생성됐습니다.
- 선택 입력이 비어 있을 때도 `미기재` 섹션과 `제출 체크리스트`가 문서에 고정 노출되어 실제 제출 결과 확인 시 노이즈가 컸습니다.
- 스킬이 `.agents/skills`, `.claude/skills`, `skills/`에 복제되어 있어 동작/출력 일관성 유지가 필요했습니다.

### 결정
- `vibecoding-result.mdx` frontmatter 생성 규칙을 변경해 제목에서 `결과 문서` 표현을 제거하고 설명 고정 문구를 단순화합니다.
- 선택 섹션(`데모 설명`, `기술 스택`, `실행/검증 방법`, `라이선스/출처`, `발표 자료`, `추가 링크`)은 값이 비어 있거나 `미기재`면 렌더링하지 않도록 스크립트에 조건부 섹션 생성 함수를 도입합니다.
- 템플릿에서 `제출 체크리스트` 섹션을 제거하고, 검증 규칙은 코어 필수 섹션 중심으로 조정합니다.
- 동일한 스킬 변경사항을 `.agents/skills/hackathon-submission`, `.claude/skills/hackathon-submission`, `skills/hackathon-submission`에 동기화합니다.

### 결과
- 생성 문서가 간결해지고 `미기재` 노출이 사라져 결과 문서 가독성이 향상됩니다.
- 사용자 요청 문구 제거/섹션 제거 요구를 스킬 수준에서 재현 가능하게 충족합니다.

## ADR-014 제출 결과 문서에서 AI 사용 내역 섹션 비노출

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- 결과 문서는 참가자가 제출한 산출물 내용을 빠르게 확인하는 용도이며, 사용자 관점에서 AI 사용 내역 섹션은 필수 열람 정보가 아니었습니다.
- 운영 요청으로 `AI 사용 여부 및 검증 방식` 섹션을 최종 노출 문서에서 제외할 필요가 있었습니다.

### 결정
- `hackathon-submission` 템플릿에서 `AI 사용 여부 및 검증 방식` 섹션 placeholder를 제거합니다.
- 생성 스크립트의 필수 섹션 검증 목록에서 AI 섹션을 제외합니다.
- 스킬 가이드/레퍼런스 문서의 권장 입력/출력 매핑에서 AI 섹션 노출 항목을 제거합니다.
- 기존 샘플 문서(`test-project`)에서도 AI 섹션을 삭제해 출력 정책을 맞춥니다.

### 결과
- 최종 결과 문서가 사용자 관점의 핵심 정보 중심으로 정리됩니다.
- 스킬 실행 시 생성되는 문서 포맷과 기존 예시 문서 포맷이 일관됩니다.

## ADR-015 바이브 코딩 결과 카드 목록의 팀 페이지 통합

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- 바이브 코딩 결과 카드 목록이 `/vibe-coding`에 별도로 존재하는 동안, 사용자는 이미 `/team`에서 목록 중심 탐색을 수행하고 있었습니다.
- 목록 진입점이 분리되면서 팀 정보와 결과 문서 정보를 한 화면에서 확인하기 어렵고 내비게이션이 중복되는 문제가 있었습니다.

### 결정
- `docs/vibe-coding/**` 결과 문서 카드 목록을 `/team` 페이지 하단 섹션으로 통합합니다.
- `/vibe-coding` 경로는 독립 카드 목록을 제거하고 `/team`으로 리다이렉트합니다.
- 상단 내비게이션의 `결과` 항목은 제거해 팀 목록 단일 진입 구조로 정리합니다.

### 결과
- 팀 정보 카드와 결과 문서 카드를 한 화면(`/team`)에서 연속적으로 확인할 수 있습니다.
- 중복 라우트/내비게이션이 줄어들어 탐색 흐름이 단순해집니다.

## ADR-016 docs 사이드바의 바이브 코딩 구간 분리와 팀 목록 바로가기

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- docs 사이드바에서 `바이브 코딩 결과`는 기존 운영 문서 메뉴와 성격이 달라 시각적 구분이 필요했습니다.
- 사용자 관점에서 결과 탐색은 `/team`이 메인 진입점이므로 docs 내에서도 바로 이동할 수 있는 링크가 필요했습니다.

### 결정
- `/docs` 사이드바 트리에서 `바이브 코딩 결과` 노드 직전에 구분선(`separator`)을 삽입합니다.
- 구분선 바로 아래에 `/team`으로 이동하는 `바이브 코딩 목록` 메뉴를 삽입합니다.
- 해당 메뉴는 외부 링크가 아닌 내부 링크로 동작하도록 설정합니다.

### 결과
- 문서 메뉴와 결과 메뉴가 시각적으로 분리되어 탐색 맥락이 명확해집니다.
- docs 사이드바에서 팀 결과 목록(`/team`)으로 즉시 이동할 수 있습니다.

## ADR-017 바이브 코딩 제출 카드의 team 컬렉션 단일 목록 통합

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- `/team` 하단에 별도 섹션으로 결과 문서 카드를 추가하면 UI 형태가 달라지고 카드 목록이 이원화되었습니다.
- 요청사항은 결과 카드가 기존 팀 프로젝트 카드와 동일한 형식으로 같은 목록에 포함되는 것이었습니다.

### 결정
- `/team` 페이지의 별도 결과 섹션을 제거하고, 기존 `contents/team` 기반 카드 그리드만 사용합니다.
- `hackathon-submission` 스킬을 확장해 제출 시 `contents/team/submission-<team-slug>-<project-slug>.mdx`를 함께 생성합니다.
- 생성되는 team 문서는 카드 렌더링에 필요한 핵심 필드(`name`, `role`, `bio`, `projectName`, `projectSummary`, `repositoryUrl`, `submittedAt`)를 채우고, URL성 선택 필드는 유효한 경우만 채웁니다.
- 기존 제출 샘플(`chlrb/test-project`)에도 team 카드 문서를 추가해 현재 목록에 즉시 반영합니다.

### 결과
- `/team`의 단일 카드 목록에서 기존 팀 카드와 제출 결과 카드가 동일한 UI 형식으로 함께 노출됩니다.
- 제출 자동화 스킬이 `/team` 노출에 필요한 데이터를 직접 생성하므로 수동 후처리 부담이 줄어듭니다.

## ADR-018 바이브 코딩 결과 문서 경로의 프로젝트 단일 레벨 정규화

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- 기존 바이브 코딩 결과 문서는 `contents/docs/vibe-coding/<team>/<project>/vibecoding-result.mdx` 형태로 중첩되어 경로 깊이가 과도했습니다.
- `/docs/vibe-coding` 탐색 시 실제 구분 기준은 팀보다 프로젝트여서, 팀/프로젝트 이중 트리는 정보 구조와 맞지 않았습니다.
- 제출 스킬 출력 경로와 실제 문서 구조가 분리되면 신규 제출 문서가 다시 중첩 경로로 생성될 위험이 있었습니다.

### 결정
- 결과 문서 경로를 `contents/docs/vibe-coding/<project-slug>.mdx`로 단일 레벨화합니다.
- 자산 경로는 `contents/docs/vibe-coding/assets/<project-slug>/{demo,evidence,team}`로 통일합니다.
- `contents/docs/vibe-coding/meta.json`의 `pages`에는 팀 슬러그가 아닌 프로젝트 슬러그를 직접 등록합니다.
- `hackathon-submission` 스킬(`.agents`, `.claude`, `skills` 복제본 포함)의 출력 규약/검증/문서 예시를 위 경로로 동기화합니다.
- 기존 샘플(`chlrb/test-project`)은 신규 경로(`test-project.mdx`, `assets/test-project/**`)로 마이그레이션하고 기존 중첩 파일은 제거합니다.

### 결과
- `/docs/vibe-coding` 구조가 프로젝트 기준 단일 레벨로 단순화되어 탐색성과 유지보수성이 개선됩니다.
- 제출 자동화와 저장소 실데이터 경로가 일치해 신규 제출 시 경로 회귀를 방지할 수 있습니다.

## ADR-019 팀 카드 목록 정렬 기준을 등록 시각 내림차순으로 전환

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- `/team` 목록은 기존에 `order` 오름차순 기반 정렬이었고, 최근 제출 카드가 목록 하단에 위치할 수 있어 최신 등록물을 바로 확인하기 어려웠습니다.
- 운영 요청은 등록 순서 기준으로 최신 제출이 먼저 노출되는 방식이었습니다.

### 결정
- `submittedTeams` 정렬 기준을 `submittedAt` 내림차순으로 변경합니다.
- `submittedAt`이 없거나 파싱 불가한 항목은 하단으로 보냅니다.
- 동률 또는 시각 미기재 항목 간 보조 정렬은 팀명(`name`) 오름차순을 사용합니다.

### 결과
- `/team`에서 최신 등록 카드가 상단에 표시되어 운영/검수 동선이 단순해집니다.
- 기존 카드 스키마를 유지하면서 정렬 기준만 변경해 영향 범위를 최소화합니다.

## ADR-020 팀 카드 클릭을 team 컬렉션 상세 문서 라우트로 고정

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- `/team` 카드의 프로젝트 링크는 기존에 `/docs/vibe-coding/<project-slug>`를 추정해 연결했기 때문에 실제 팀 제출 파일 경로와 불일치할 수 있었습니다.
- 특히 `contents/team/submission-chlrb-test-project.mdx` 같은 제출 카드 파일은 `/team`에서 바로 열람되는 상세 라우트가 없어 클릭 동선이 끊겼습니다.

### 결정
- `/team` 카드의 이미지/프로젝트명 링크를 `projectName` 추정 slug가 아닌 `team.info.path` 기반 경로(`/team/<submission-doc-slug>`)로 변경합니다.
- App Router에 `/team/[slug]` 상세 라우트를 추가해 `contents/team` 컬렉션 문서 본문과 TOC를 그대로 렌더링합니다.
- 상세 페이지 메타데이터는 `projectName`/`projectSummary`를 우선 사용하고, 값이 없을 때 `name`/`bio`를 대체값으로 사용합니다.

### 결과
- `/team`에서 프로젝트를 클릭하면 해당 팀 제출 파일(`submission-...mdx`)을 직접 렌더링하는 상세 페이지로 안정적으로 이동합니다.
- 제출 카드 링크가 파일 경로와 1:1 매핑되어 slug 추정 오류가 제거됩니다.

## ADR-021 GNB FAQ 메뉴를 문서 경로로 정렬

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- 상단 GNB의 FAQ 링크가 `/faq`를 가리켜, 문서 허브(`/docs`) 기반 정보 구조와 진입 경로가 분리되어 있었습니다.
- 행사 안내 문서는 `/docs/[slug]` 구조를 기준으로 운영되고 있어, FAQ도 동일한 정보 설계 안에서 접근되는 것이 일관성 측면에서 유리했습니다.

### 결정
- `src/lib/site-config.ts`의 GNB `FAQ` 메뉴 링크를 `/faq`에서 `/docs/faq`로 변경합니다.
- GNB는 FAQ 전용 페이지가 아닌 문서 경로를 단일 진입점으로 사용합니다.

### 결과
- 사용자는 상단 내비게이션에서 FAQ를 클릭하면 문서 페이지(`/docs/faq`)로 바로 이동합니다.
- 문서 인덱스/상세 라우팅 체계와 GNB 진입 경로가 일관되게 정렬됩니다.

## ADR-022 운영 메인 브랜치에서 test-project 샘플 제출 제거

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- `/team`은 운영 노출 페이지이므로 실제 참가팀/제출 데이터 중심으로 유지되어야 합니다.
- 검증용 샘플 제출(`submission-chlrb-test-project.mdx`)이 메인 브랜치에 포함되어 `/team`에서 실제 제출처럼 노출되고 있었습니다.

### 결정
- `contents/team/submission-chlrb-test-project.mdx` 파일을 메인 브랜치에서 제거합니다.
- `/team` 상세 라우팅/카드 렌더링 규칙은 유지하되, 운영 데이터에서는 테스트 샘플을 제외합니다.

### 결과
- `/team` 목록에서 `test-project` 샘플 카드가 노출되지 않습니다.
- 운영 페이지의 제출 목록이 실제 데이터 중심으로 유지됩니다.

## ADR-023 LLM 안내용 `llms.txt` 고정 텍스트 라우트 도입

- 상태: 승인
- 날짜: 2026-02-20

### 배경
- 외부 LLM/에이전트가 행사 기본 안내와 제출 준비 절차를 빠르게 수집하려면 단일 텍스트 엔드포인트가 필요했습니다.
- 기존 안내는 MDX 문서 중심이라 렌더링 컨텍스트(레이아웃/컴포넌트/내비게이션)에 따라 파싱 복잡도가 높아질 수 있었습니다.

### 결정
- App Router에 `GET /llms.txt` 라우트를 추가하고, 응답 형식을 `text/plain; charset=utf-8`로 고정합니다.
- 본문은 동적 파싱 대신 정적 고정 텍스트를 사용하며, `행사 기본소개`, `사전 준비사항`, `제출 준비사항` 3개 섹션을 상세 문장으로 제공합니다.
- `사전 준비사항`은 저장소 운영 원칙 중심으로 구성하고, `필수 준비물` 및 `사전 권장 준비` 나열은 제외합니다.
- 링크는 `https://vibecoding.okky.kr` 기반 절대 URL만 사용하고, 운영 문서 변경 시 `llms.txt`를 수동 동기화합니다.

### 결과
- LLM이 단일 경로(`/llms.txt`)에서 행사 핵심 맥락을 안정적으로 읽을 수 있습니다.
- 구현 복잡도와 의존성을 낮추는 대신, 문서 변경 시 수동 업데이트 운영 규칙을 명시적으로 관리하게 됩니다.

## ADR-024 shadcn 레지스트리에 GooseUI 네임스페이스 추가

- 저장소는 `components.json`의 `registries`를 통해 shadcn 커스텀 레지스트리를 사용하고 있습니다.
- Footer 컴포넌트 설치 검증을 위해 GooseUI 패키지 네임스페이스(`@gooseui/...`)를 동일한 방식으로 호출할 수 있어야 했습니다.

### 결정
- `components.json`의 `registries`에 `@gooseui` 항목을 추가합니다.
- GooseUI 네임스페이스는 공식 설치 문서 기준 경로 `https://gooseui.pro/r/{name}.json`를 사용합니다.

### 결과
- 저장소에서 `pnpm dlx shadcn@latest add @gooseui/<name>` 형식의 설치 시도를 수행할 수 있습니다.
- 레지스트리 설정이 코드베이스에 명시되어 재현 가능한 설치 조건이 확보됩니다.
